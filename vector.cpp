#include <bits/stdc++.h>

#define TASK "a"
#define input_file TASK".inp"
#define output_file TASK".out"

using namespace std;

int n, u, v;
int x[50], y[50];

inline void chia1(int k) {
    for (int i = 0; i <= 1; i++) {
        if (k > n/2)
            nap1;
        else return;

    }
}

int main()
{
    freopen(input_file, "r", stdin);
    freopen(output_file, "w", stdout);

    scanf("%d", &n);
    for (int i = 1; i <= n; i++)
        scanf("%d %d", &x[i], &y[i]);
    scanf("%d %d", &u, &v);
    chia1(1);
}
